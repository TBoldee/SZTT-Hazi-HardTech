<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="VotingPlatform.View.AllPollsView"
             xmlns:viewModel="clr-namespace:VotingPlatform.ViewModel"
             xmlns:model="clr-namespace:VotingPlatform.Model"
             Title="">
    <ContentPage.Content>
		<VerticalStackLayout x:DataType="viewModel:VotingPlatformViewModel" HorizontalOptions="Center">
			<CollectionView ItemsSource="{Binding PollViewModelList}">
				<CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="viewModel:PollViewModel">
						<VerticalStackLayout>
							<Border WidthRequest="700" Margin="20" Padding="2">
								<VerticalStackLayout>
									<Label Text="{Binding Title}" FontSize="35" HorizontalOptions="Center"/>
									<Label Text="{Binding Description}" FontSize="20"/>
									<HorizontalStackLayout Spacing="5">
										<Label Text="Created at:" FontSize="10"/>
										<Label Text="{Binding CreatedAt}" FontSize="10"/>
									</HorizontalStackLayout>
									<HorizontalStackLayout Spacing="5">
										<Label Text="Closes at:" FontSize="10"/>
										<Label Text="{Binding ClosedAt}" FontSize="10"/>
									</HorizontalStackLayout>
									<CollectionView ItemsSource="{Binding Options}">
										<CollectionView.ItemTemplate>
											<DataTemplate x:DataType="viewModel:VoteOptionViewModel">
												<VerticalStackLayout>
													<Border Padding="2">
														<Button Text="{Binding Text}" BackgroundColor="Transparent"/>
													</Border>
												</VerticalStackLayout>
											</DataTemplate>
										</CollectionView.ItemTemplate>
									</CollectionView>
								</VerticalStackLayout>
							</Border>
						</VerticalStackLayout>
					</DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </VerticalStackLayout>
    </ContentPage.Content>
</ContentPage>